# Place your resume PDF here

Place your actual `resume.pdf` file in this directory (`public/`).

It is referenced by the **Resume** download button in the Hero section:
```
href="/resume.pdf"
```

Next.js serves everything in `public/` at the root URL, so the link will resolve
correctly in both development and production.
